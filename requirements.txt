# Core pipeline (fetch_data.py itself) only uses Python's standard library
# (csv, json, os, datetime) — so technically nothing below is required
# until you wire up a REAL data source in fetch_price_from_source().
# Uncomment what you actually end up using; don't install what you don't need.

# --- Fetching data from an API ---
requests==2.32.3          # simplest way to call an HTTP/JSON API

# --- Scraping a webpage (only if the source has no API, and scraping is
#     permitted by that site's Terms of Service) ---
beautifulsoup4==4.12.3    # parse HTML
lxml==5.2.2                # fast HTML/XML parser used by BeautifulSoup

# --- If you want pandas for easier CSV/history handling instead of the
#     built-in csv module (optional, not required by the current script) ---
pandas==2.2.2

# --- If you later want a more advanced model than the built-in EWMA
#     (e.g. real ARIMA / Holt-Winters via statsmodels) ---
statsmodels==0.14.2
numpy==1.26.4

# --- If you add scheduled email/SMS alerts to farmers ---
# twilio==9.2.3            # SMS
